package com.nexio.catalog.error;

import com.nexio.catalog.Utils.CustomMessageType;

public class CustomErrorType extends CustomMessageType {

    public CustomErrorType(String errorMessage){
        super(errorMessage);
    }
}
